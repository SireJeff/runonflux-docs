# Hardware

From the hardware tab you can see quite a bit of information on the machine, that comes from a combination of the hardware scan and the benchmark process. If you have recently changed the system configuration and see discrepancies run a hardware rescan from the button in the upper right.

<img src="/.gitbook/assets/image (68).png" alt=""/>

&#x20;ard
